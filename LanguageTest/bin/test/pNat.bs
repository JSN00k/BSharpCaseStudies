package test

Import Relations

Datatype pNat
| zero
| suc(prev : pNat) {
	add(x, y : pNat) : pNat INFIX 100
		match x {
			| zero : y
			| suc(xs) : suc(xs add y)
		}
	
	Instance Setoid<pNat>([=])
}

Import Monoid

Extend pNat (mon_Add) {
	Instance Monoid<pNat>(add, zero) addMon
}